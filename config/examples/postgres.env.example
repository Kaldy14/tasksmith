# Copy to /opt/tasksmith/config/postgres.env on the server.
# Do not commit the real file or password.
POSTGRES_DB=tasksmith
POSTGRES_USER=tasksmith
POSTGRES_PASSWORD=replace-with-a-long-random-password
